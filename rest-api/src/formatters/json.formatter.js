/**
 * JSON Formatter
 * Returns data as-is (JSON is the default format)
 */

/**
 * Format data as JSON
 * @param {Array|Object} data - Data to format
 * @returns {Array|Object} Data as-is
 */
function format(data) {
  return data;
}

module.exports = {
  format,
};
